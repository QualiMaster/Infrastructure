package switching.logging;

/**
 * Holds identifier for different signal names.
 * 
 * @author Nowatzki
 */
public enum SignalName {

    ENACT, DISABLE, STOPPED, LASTPROCESSEDID, HEADID, TRANSFER, TRANSFERRED, EMIT, 
    GOTOACTIVE, GOTOPASSIVE, PASSIVATE, SYNCHRONIZED, COMPLETED, ENABLE, DELAY
}
